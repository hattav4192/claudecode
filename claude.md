# Claude Code Configuration

## YOU MUST:
- 回答は日本語で行ってください
- 作業前に必ず計画を立て、計画に沿って実行してください
- TODOには必ずブランチ作成・実装内容のテスト・コミット・push・PR作成（まだ作成されていない場合）が含まれるべきです

## ワークフロー

### ブランチ運用ルール
- **作業開始時**: 必ず専用ブランチを作成する（feat-<機能名>、fix-<修正内容>等）
- **mainブランチでの直接作業は絶対禁止**
- **作業終了時**: lint・typecheck・テストを実行し、全てパスしてからコミット・push・PR作成

### コミットメッセージ規約
- Conventional Commits形式: feat:, fix:, docs:, refactor:, test:, chore:
- 本文は日本語で記述

### テスト駆動
- 新機能の実装前にテストを書く（testerエージェントを活用）
- テストが全てパスすることを確認してからコミット

## コードスタイル
- 型安全性を最優先: `any`型の使用禁止
- 関数は小さく保つ（1関数50行以内を目安）
- コメントは「なぜ」を書く、「何を」は書かない
- 未使用のインポートを残さない

## プロジェクト構造
- `demo_life.py` - Conway's Game of Life デモ

## 進捗管理
- 大きなタスクの場合は `progress.md` に進捗を記録する
- 各ステップの完了状態を [x] / [ ] で管理

## Agent Teams 構成

このプロジェクトには5人のエージェントチームがあります。`.claude/agents/` で定義されています。

| エージェント | ファイル | 役割 |
|---|---|---|
| PM | `pm.md` | タスク分解・進捗管理・チーム調整 |
| Frontend | `frontend.md` | React/TypeScript UI実装 |
| Backend | `backend.md` | API設計・DB設計・サーバーサイド |
| Tester | `tester.md` | テスト自動化・カバレッジ管理 |
| DocsWriter | `docs-writer.md` | ドキュメント作成・更新 |

### チームの使い方（例）
- 新機能全体: 「PMエージェントを使って〇〇機能を実装してください」
- フロントのみ: 「Frontendエージェントで〇〇画面を作ってください」
- テストのみ: 「Testerエージェントでカバレッジを改善してください」

### Agent Teams のルール
- 並列分解可能なタスクのみTeamsを使用する
- 順次依存するタスクはLead単独で実行する
- 進捗は `progress.md` で共有する